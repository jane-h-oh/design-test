import { SermonLabWorkspace } from '@/components/sermon-lab/sermon-lab-workspace';

export default function SermonLabPage() {
  return (
    <div className="h-[calc(100vh-64px)] p-6 bg-slate-50">
      <SermonLabWorkspace />
    </div>
  );
}
